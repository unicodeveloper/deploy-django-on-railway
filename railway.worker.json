{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "celery -A liftoff worker -l info --uid=nobody --gid=nogroup",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
